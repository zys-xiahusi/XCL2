<local:MyPageRight
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:PCL"
    x:Class="PageOtherEaster"
    PanScroll="PanBack"
    RenderOptions.BitmapScalingMode="LowQuality">
    <local:MyScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" x:Name="PanBack">
        <Grid x:Name="PanMain" Margin="25,10">
            <Border Background="{DynamicResource ColorBrushBg1}" CornerRadius="8" Padding="40">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 主文字 -->
                    <TextBlock Grid.Row="0"
                               Text="「25年的5月25日，奇迹的开始」"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ColorBrush2}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center" />

                    <!-- 右下角二维码 -->
                    <Image Grid.Row="1"
                           Source="/Plain Craft Launcher 2;component/Images/Heads/qrcode.png"
                           Width="100"
                           Height="100"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Margin="0,20,0,0" />
                </Grid>
            </Border>
        </Grid>
    </local:MyScrollViewer>
</local:MyPageRight>
